// export function trigger_prompt_event(prompt) {
//     const event = new CustomEvent("prompt-update", { detail: prompt });

//     const terminal = document.querySelectorAll();
//     this.dispatchEvent(event);
// }